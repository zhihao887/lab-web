# 实验室官网从构建到上线：一次完整流程分享

> 适用场景：组会分享  
> 建议时长：20-30 分钟  
> 分享重点：讲清楚“一个网站如何从本地页面变成大家都能访问的线上官网”  
> 讲述原则：少讲代码细节，多讲流程、选择、问题和经验  

---

## 一、分享定位

这次分享不把重点放在“我写了多少代码”上，而是把整个官网上线过程拆成一条完整链路：

```
内容准备
  -> 前端页面构建
  -> 本地调试
  -> 打包生成静态文件
  -> Vercel 部署
  -> 自定义域名绑定
  -> Cloudflare DNS 托管
  -> 国内访问验证
```

我希望大家听完以后，能对这件事形成一个整体认识：

1. 官网不是一开始就“部署”出来的，而是先把内容、页面、数据结构组织好。
2. 前端框架的作用不是炫技，而是把重复页面、动态列表、详情页和交互统一管理起来。
3. Vercel 解决的是“如何快速把静态网站发布到公网”。
4. Cloudflare 解决的是“域名解析、代理、缓存、访问稳定性和安全层”的问题。
5. 真正难的地方往往不是某段代码，而是把本地开发、线上部署、域名解析、国内网络环境串成一条能跑通的流程。

---

## 二、建议分享节奏

| 环节 | 建议时长 | 重点 |
|---|---:|---|
| 开场：为什么做这个官网 | 2 分钟 | 项目背景、目标、最终效果 |
| 第一部分：前端页面如何搭起来 | 7-8 分钟 | Vue + Vite、页面结构、数据驱动、组件复用 |
| 第二部分：如何从本地变成可部署文件 | 3-4 分钟 | 本地调试、构建、dist 静态文件 |
| 第三部分：Vercel 部署 | 5 分钟 | 自动构建、线上地址、SPA 404 问题 |
| 第四部分：Cloudflare DNS 与国内访问 | 6-8 分钟 | 域名、DNS、代理、访问链路 |
| 第五部分：踩坑和经验 | 4 分钟 | 404、图片体积、国内访问、内容维护 |
| 总结与 Q&A | 3 分钟 | 一句话总结、后续优化 |

---

## 三、项目概况

本项目是一个实验室/研究团队官网，用于展示“问题研究社”的研究方向、研究问题、团队成员、论文成果、动态、项目、资源和加入方式。

从网站定位上看，它不是一个复杂的后台系统，而是一个展示型、内容型、学术型官网。它的核心诉求有三个：

1. 信息清楚：让别人快速知道我们是谁、做什么、有哪些方向和成果。
2. 维护方便：后续成员、论文、问题库更新时，不需要大改页面。
3. 访问稳定：不只自己电脑能打开，而是校内外、国内网络环境下都能尽量稳定访问。

当前项目的真实技术结构：

| 模块 | 项目中对应内容 | 面向听众的解释 |
|---|---|---|
| 前端框架 | Vue 3 | 用组件化方式搭页面 |
| 构建工具 | Vite | 负责本地开发和打包 |
| 路由 | Vue Router | 控制不同网址显示不同页面 |
| 样式 | 原生 CSS | 控制整体视觉、响应式和交互效果 |
| 数据 | `src/data/siteData.js` | 集中管理全站内容 |
| 部署 | Vercel | 把网站发布到公网 |
| 域名与 DNS | Cloudflare | 管理域名解析和访问链路 |

项目规模可以这样讲：

| 指标 | 当前项目 |
|---|---:|
| 页面视图 | 10 个 |
| 公共组件 | 4 个 |
| 核心数据文件 | 约 3054 行 |
| 全局样式文件 | 约 2731 行 |
| 构建后 JS | 约 262 KB |
| 构建后 CSS | 约 60 KB |
| 图片资源 | 约 5.6 MB |

这组数据的意义不是“代码很多”，而是说明一个完整官网通常由三类东西组成：页面结构、内容数据和视觉样式。

---

## 四、第一部分：前端页面如何搭起来

### 4.1 先从最普通的问题开始：官网需要哪些页面

一开始不应该先想“用什么技术”，而应该先想“官网要表达什么”。

这个项目最终形成了 7 个主要页面和 3 类详情页：

| 页面 | 作用 |
|---|---|
| 首页 | 总览实验室定位、方向、数据、入口 |
| 团队页 | 展示成员与研究方向 |
| 成果页 | 展示论文与学术产出 |
| 动态页 | 展示新闻、活动和进展 |
| 项目页 | 展示研究问题和项目 |
| 资源页 | 汇总数据、代码、文档、演示入口 |
| 加入我们 | 面向新同学和协作者 |
| 研究方向详情页 | 展开某个方向的具体问题 |
| 论文详情页 | 展示单篇成果和 BibTeX |
| 项目详情页 | 展示单个研究问题的细节 |

分享时可以这样讲：

> 我在做页面之前，先把官网当成一个“信息导览系统”。用户进入网站后，应该能从首页知道我们整体做什么，再通过导航进入团队、成果、问题库和加入方式。这样页面结构就不是随便堆出来的，而是围绕访问者的路径组织的。

### 4.2 为什么使用 Vue 3 + Vite

这里不要展开讲 Vue 的响应式原理，只讲选择原因。

可以这样表达：

> 这个官网虽然没有后台登录和数据库，但有很多重复结构，比如成员卡片、论文列表、项目列表、详情页。如果只写普通 HTML，后续维护会很麻烦。Vue 的价值是把页面拆成组件，把重复展示变成可复用模板；Vite 的价值是让本地开发和最终打包都很快。

选择 Vue 3 + Vite 的原因：

| 选择 | 原因 |
|---|---|
| Vue 3 | 组件化直观，上手快，适合中小型展示网站 |
| Vite | 启动快、打包简单、默认配置轻 |
| Vue Router | 可以让 `/people`、`/publications`、`/join` 这些地址对应不同页面 |
| 原生 CSS | 页面视觉定制程度高，不受组件库风格限制 |

可以用一句话概括：

> Vue 负责把页面拆清楚，Vite 负责把项目跑起来和打包，Router 负责让不同网址对应不同页面。

### 4.3 项目目录怎么理解

不用逐个文件讲代码，可以用“分工”的方式讲：

```
src/
  main.js              项目入口
  App.vue              网站外壳：顶部导航 + 页面内容 + 底部
  router/index.js      路由表：网址和页面的对应关系
  data/siteData.js     全站内容数据
  components/          公共组件
  views/               具体页面
  styles/main.css      全局样式
```

讲述方式：

> 这个结构其实很像一本书。`App.vue` 是书壳，`router` 是目录，`views` 是每一章，`components` 是反复出现的版式模块，`siteData.js` 是所有正文素材，`main.css` 决定整本书的视觉风格。

### 4.4 数据驱动：这个项目最值得分享的设计

项目里最重要的文件是：

```
src/data/siteData.js
```

它集中管理了：

| 数据类型 | 内容 |
|---|---|
| `siteInfo` | 实验室名称、英文名、邮箱、地点 |
| `navItems` | 顶部导航 |
| `stats` | 首页统计数字 |
| `researchProblems` | 研究问题库 |
| `publications` | 论文成果 |
| `news` | 动态新闻 |
| `resources` | 资源入口 |
| `people` | 团队成员 |

分享重点：

> 我没有把每个成员、每篇论文、每个项目都写死在页面里，而是把它们集中放进一个数据文件。页面只负责“怎么显示”，数据文件负责“显示什么”。这样后续如果新增一个成员，只需要改数据；如果要调整页面样式，只需要改组件和 CSS。

可以用这个对比解释：

| 写死在页面里 | 数据驱动 |
|---|---|
| 每改一条内容都要找页面代码 | 内容统一在数据文件里改 |
| 页面和内容混在一起 | 页面负责展示，数据负责内容 |
| 后期维护容易乱 | 后期扩展更清晰 |

适合放在 PPT 的一句话：

> 内容和页面分离，是这个官网后续能持续维护的关键。

### 4.5 组件复用：减少重复劳动

项目中有 4 个公共组件：

| 组件 | 作用 |
|---|---|
| `LabHeader.vue` | 顶部导航 |
| `SiteFooter.vue` | 底部页脚 |
| `PageHero.vue` | 各页面顶部标题区 |
| `HeroVisual.vue` | 首页视觉区域 |

讲述方式：

> 官网里有一些元素每一页都会出现，比如顶部导航和底部页脚。如果每个页面都复制一遍，后续改导航就要改很多地方。组件化之后，只要改一个组件，全站都会同步更新。

可以举一个容易理解的例子：

> 比如导航栏增加“资源”入口，如果导航栏是复制在每个页面里的，就要改 7 次；现在它是一个组件，只改一次。

### 4.6 路由：让网址和页面对应起来

项目中使用 Vue Router，把不同路径映射到不同页面：

| 网址 | 页面 |
|---|---|
| `/` | 首页 |
| `/people` | 团队 |
| `/publications` | 成果 |
| `/news` | 动态 |
| `/projects` | 项目 |
| `/resources` | 资源 |
| `/join` | 加入我们 |
| `/research/:slug` | 研究方向详情 |
| `/publications/:id` | 论文详情 |
| `/projects/:id` | 项目详情 |

这里可以讲一个很重要但不太技术化的点：

> 用户看到的是不同网址，实际上服务器最开始返回的都是同一个入口页面 `index.html`。进入浏览器以后，再由前端路由决定显示哪个页面。这就是单页应用的基本模式。

这一点后面讲 Vercel 404 时会用到。

### 4.7 页面设计：不是为了花哨，而是为了降低理解成本

这个官网的视觉目标不是做成商业广告页，而是做成一个学术团队的展示窗口。

设计上有几个取向：

1. 首页先给总体定位和核心入口。
2. 团队、成果、项目等页面采用列表和卡片，便于扫描。
3. 研究方向用不同主题色区分，减少信息混乱。
4. 移动端适配，保证手机上也能看。
5. 加入我们页面做成申请导向，让新同学知道下一步该做什么。

可以这样说：

> 前端页面设计不是单纯追求“好看”，而是让访问者少思考。一个第一次来的同学，应该很快知道：这个团队做什么、谁在做、有哪些成果、我怎么加入。

---

## 五、第二部分：从本地开发到静态文件

### 5.1 本地开发阶段

本地开发时使用：

```bash
npm run dev
```

它做的事情可以这样讲：

> 这个命令会在本地启动一个开发服务器。我们每次修改页面、样式或数据，浏览器都会快速刷新，方便边改边看。

本地开发阶段主要检查：

1. 页面能不能正常打开。
2. 导航是否跳转正确。
3. 内容是否显示完整。
4. 手机尺寸是否变形。
5. 图片、按钮、链接是否正常。

### 5.2 打包阶段

当本地没问题后，执行：

```bash
npm run build
```

它会生成：

```
dist/
  index.html
  assets/
    index-xxx.js
    index-xxx.css
  images/
```

讲述方式：

> 写项目时我们看到的是 Vue 文件、CSS 文件、数据文件。但浏览器最终需要的是普通 HTML、JavaScript、CSS 和图片。Vite 打包的过程，就是把开发时的项目结构转换成浏览器可以直接访问的一组静态文件。

可以用类比：

> 本地开发像是在编辑 Word 文档，打包就像导出 PDF。别人访问网站时，不需要看到我们的开发工程，只需要拿到最终导出的静态文件。

### 5.3 为什么这个项目适合做静态站

这个官网目前不需要：

1. 用户登录。
2. 实时数据库。
3. 后台管理。
4. 在线支付。
5. 服务端计算。

所以它非常适合做成纯静态站。

纯静态站的好处：

| 好处 | 解释 |
|---|---|
| 成本低 | 不需要服务器长期运行后端程序 |
| 安全性高 | 没有登录系统和数据库，攻击面小 |
| 加载快 | 文件可以直接缓存到 CDN |
| 部署简单 | 上传静态文件即可 |
| 维护轻 | 改内容后重新构建发布 |

适合放在 PPT 的一句话：

> 展示型官网优先考虑静态化，可以用最低运维成本获得很好的访问速度和稳定性。

---

## 六、第三部分：Vercel 部署

### 6.1 Vercel 在这条链路里负责什么

Vercel 的角色可以理解为：

> 它帮我们把本地打包出来的网站文件，发布到全球可以访问的网络上，并自动配置 HTTPS、构建和 CDN。

如果不用 Vercel，就需要自己做很多事情：

1. 租服务器。
2. 配 Nginx。
3. 上传文件。
4. 配 HTTPS 证书。
5. 配缓存。
6. 更新时手动发布。

使用 Vercel 后，大部分变成自动流程：

```
代码提交
  -> Vercel 拉取代码
  -> 安装依赖
  -> 执行 npm run build
  -> 发布 dist
  -> 生成线上地址
```

### 6.2 部署流程可以怎么讲

比较适合组会的讲法：

1. 先把项目上传到 Git 仓库。
2. 在 Vercel 中导入项目。
3. Vercel 自动识别这是 Vite 项目。
4. 构建命令使用 `npm run build`。
5. 输出目录是 `dist`。
6. 部署成功后获得一个默认域名。
7. 后续每次更新代码，Vercel 可以自动重新构建和上线。

可以用这张流程图：

```
本地修改代码
  -> git push
  -> Vercel 自动构建
  -> 生成静态资源
  -> 分发到 CDN
  -> 用户访问最新网站
```

### 6.3 Vercel 部署里最关键的配置：刷新不 404

项目中有一个文件：

```
vercel.json
```

内容是：

```json
{
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
```

这段配置可以不用逐行讲，只讲它解决的问题：

> 因为我们用的是前端路由，用户访问 `/people` 时，服务器上其实没有一个叫 `people` 的真实文件。如果没有特殊配置，刷新页面时服务器会找不到文件，返回 404。这个配置的意思是：不管用户访问哪个路径，都先返回 `index.html`，再让 Vue Router 在浏览器里决定显示哪个页面。

可以用一个清晰例子：

```
用户访问 https://xxx.com/people
  -> Vercel 收到请求
  -> 发现没有 /people 这个物理文件
  -> 按 rewrites 配置返回 index.html
  -> 浏览器加载 Vue 应用
  -> Vue Router 渲染团队页
```

这一点是分享里很值得讲的“实际踩坑”。

### 6.4 Vercel 的优势和限制

优势：

| 优势 | 说明 |
|---|---|
| 上线快 | 从导入项目到部署成功很快 |
| 自动 HTTPS | 不需要手动申请证书 |
| 自动构建 | 推送代码后自动发布 |
| 免费额度够用 | 对实验室展示站基本足够 |
| 适合前端项目 | 对 Vite、Vue、React 等支持很好 |

限制：

| 限制 | 表现 |
|---|---|
| 默认域名国内不稳定 | `vercel.app` 在国内网络环境下可能访问失败 |
| 后端能力不是本项目重点 | 本项目主要是静态站 |
| 图片体积仍需自己优化 | 部署平台不会自动解决所有性能问题 |

过渡句：

> 到这一步，网站已经能上线了。但问题是：它能不能稳定地被国内用户访问？这就进入下一层，域名和 DNS。

---

## 七、第四部分：Cloudflare DNS 托管与国内访问

### 7.1 为什么部署成功还不够

很多人会以为“Vercel 部署成功 = 网站上线完成”，但实际访问链路里还有一个关键角色：域名解析。

用户访问网站时，大致发生了这些事：

```
用户输入域名
  -> DNS 查询这个域名对应哪里
  -> 浏览器连接对应服务器或 CDN
  -> 下载 HTML / CSS / JS / 图片
  -> 页面渲染
```

如果 DNS 解析不稳定，或者默认域名在某些网络环境下被影响，那么即使网站在 Vercel 上部署成功，用户也可能打不开。

可以这样讲：

> 部署解决的是“网站文件放在哪里”，DNS 解决的是“用户输入域名后怎么找到它”。

### 7.2 为什么要使用自定义域名

Vercel 默认会给一个类似这样的地址：

```
xxx.vercel.app
```

它适合测试，但不适合作为正式官网主地址。

原因：

1. 品牌感弱，不像正式官网。
2. 国内访问稳定性可能受影响。
3. 不方便传播和记忆。
4. 后续迁移平台时，默认域名不可控。

使用自定义域名后，例如：

```
imblab.cn
www.imblab.cn
```

好处是：

1. 访问入口统一。
2. 更正式。
3. 后续即使部署平台变化，域名也可以继续使用。
4. 可以通过 Cloudflare 统一管理 DNS、缓存和安全策略。

### 7.3 Cloudflare 在这里扮演什么角色

Cloudflare 可以理解成域名访问链路中的“中转和管理层”。

它主要做几件事：

| 能力 | 作用 |
|---|---|
| DNS 托管 | 管理域名指向哪里 |
| 代理访问 | 让用户请求先经过 Cloudflare |
| HTTPS | 提供证书和加密访问 |
| CDN 缓存 | 缓存静态资源，提高访问速度 |
| 安全防护 | 隐藏源站、过滤异常请求 |

讲给非技术听众可以这样说：

> Vercel 是放网站的地方，Cloudflare 是用户访问网站时经过的一层入口。用户不需要知道源站在哪里，只需要访问我们的域名；Cloudflare 再把请求转到 Vercel 或直接返回缓存内容。

### 7.4 访问链路对比

只使用 Vercel 默认域名：

```
用户
  -> 解析 xxx.vercel.app
  -> 访问 Vercel
  -> 下载网站
```

使用自定义域名 + Cloudflare：

```
用户
  -> 访问 imblab.cn
  -> Cloudflare DNS 解析
  -> Cloudflare 代理 / 缓存
  -> 回源到 Vercel
  -> 返回网站内容
```

可以强调：

> Cloudflare 不是把网站代码重新部署了一遍，而是站在用户和 Vercel 中间，负责域名解析、代理、缓存和安全。

### 7.5 具体配置流程

#### 第一步：购买或准备域名

例如：

```
imblab.cn
```

域名的作用是提供一个稳定、可记忆、可长期使用的入口。

注意点：

1. 如果网站部署在境外平台，通常不需要国内服务器备案。
2. 如果未来接入国内服务器或大陆 CDN，通常需要考虑 ICP 备案。
3. 域名实名、解析生效、证书签发都需要一定等待时间。

#### 第二步：在 Cloudflare 添加站点

流程：

1. 登录 Cloudflare。
2. Add a site。
3. 输入域名。
4. 选择 Free 计划。
5. Cloudflare 扫描现有 DNS 记录。

这一步的本质：

> 告诉 Cloudflare：这个域名以后由你帮我管理解析。

#### 第三步：修改域名注册商的 NS 服务器

Cloudflare 会给出两条 nameserver，例如：

```
xxx.ns.cloudflare.com
yyy.ns.cloudflare.com
```

需要去域名注册商后台，把原来的 DNS 服务器改成 Cloudflare 提供的这两条。

这一点可以这样解释：

> 域名注册商负责“我拥有这个域名”，Cloudflare 负责“这个域名具体指向哪里”。把 NS 改到 Cloudflare，就是把解析管理权交给 Cloudflare。

生效时间：

1. 快的时候十几分钟。
2. 慢的时候 24-48 小时。
3. 不同网络运营商缓存不同，所以有的人先能访问，有的人晚一点。

#### 第四步：在 Cloudflare 添加 DNS 记录

常见配置：

| 类型 | 名称 | 目标 | 代理状态 |
|---|---|---|---|
| CNAME | `@` | `cname.vercel-dns.com` | Proxied |
| CNAME | `www` | `cname.vercel-dns.com` | Proxied |

也可以根据 Vercel 后台提示配置 A 记录或 CNAME 记录，实际以 Vercel 给出的域名验证提示为准。

讲述重点：

> DNS 记录就是告诉全世界：访问我的域名时，应该去哪里找网站。开启 Cloudflare 的橙色云代理后，请求会先经过 Cloudflare，再转发到 Vercel。

#### 第五步：在 Vercel 添加自定义域名

在 Vercel 项目的 Settings -> Domains 中添加：

```
imblab.cn
www.imblab.cn
```

Vercel 会验证 DNS 是否正确。

如果验证通过：

1. Vercel 知道这个域名属于当前项目。
2. HTTPS 证书会自动处理。
3. 用户访问该域名时，Vercel 能返回正确项目。

#### 第六步：验证访问

可以做三类验证：

1. 浏览器直接访问首页。
2. 访问二级页面，例如 `/people`、`/join`，再刷新。
3. 用不同网络测试，例如校园网、手机流量、不同地区网络。

也可以用命令验证：

```bash
nslookup imblab.cn
curl -I https://imblab.cn
```

分享时不用展示太多命令，展示结果即可：

> 重点不是命令本身，而是确认三件事：域名能解析、HTTPS 正常、二级页面刷新不 404。

### 7.6 关于“国内访问”的表达要准确

组会上建议这样讲：

> 通过自定义 `.cn` 域名和 Cloudflare DNS/代理，可以绕开 Vercel 默认域名在国内访问不稳定的问题，并显著提升访问成功率和访问体验。但如果未来要追求企业级稳定，最稳妥的方案仍然是备案后接入国内 CDN 或国内云服务。

这样表达更严谨。

可以补充：

1. 当前方案优点是成本低、配置快、维护轻。
2. 它适合实验室官网、个人项目、学术展示站。
3. 如果未来访问量很大、对国内稳定性要求极高，可以升级为“备案域名 + 国内 CDN + 静态资源镜像”。

---

## 八、第五部分：遇到的问题和解决方法

### 8.1 问题一：刷新二级页面 404

现象：

> 从首页点击“团队”可以正常进入 `/people`，但直接刷新 `/people` 页面时可能 404。

原因：

> 前端路由地址不是服务器上的真实文件，服务器默认找不到 `/people`。

解决：

> 在 `vercel.json` 中配置 rewrites，让所有路径都返回 `index.html`，再由 Vue Router 渲染对应页面。

分享价值：

> 这个问题说明，本地能跑不代表线上就完全没问题。部署时要理解浏览器、路由和服务器之间的关系。

### 8.2 问题二：图片资源偏大

现象：

> 构建后的图片资源约 5.6 MB，比 JS 和 CSS 大得多。手机网络下图片可能成为主要加载瓶颈。

解决方向：

1. PNG 转 WebP 或 AVIF。
2. 非首屏图片使用懒加载。
3. 按不同屏幕提供不同尺寸图片。
4. 重要页面优先压缩首屏图片。

分享价值：

> 前端性能不只看代码体积，图片往往才是展示型网站最大的资源成本。

### 8.3 问题三：国内访问不稳定

现象：

> Vercel 默认域名在部分国内网络环境下访问不稳定。

解决：

1. 使用自定义域名。
2. 将 DNS 托管到 Cloudflare。
3. 开启代理和缓存。
4. 多网络环境测试。

分享价值：

> 网站上线不是“自己能打开”就结束，真实用户在不同网络环境中的访问体验才是最终标准。

### 8.4 问题四：内容后续维护

现象：

> 成员、论文、项目和动态会持续变化，如果每次都改页面代码，后续维护成本高。

当前解决：

> 使用 `siteData.js` 集中管理全站数据。

后续可优化：

1. 从 Excel 自动生成数据文件。
2. 引入无头 CMS，让非技术成员通过后台更新。
3. 把论文、成员、项目拆成多个数据文件，降低单文件体积。

分享价值：

> 一个网站能不能长期用，不只取决于第一次上线，也取决于后续谁能维护、怎么维护。

---

## 九、适合 PPT 的完整页结构

### 第 1 页：标题页

标题：

> 实验室官网从构建到上线：从前端页面到 Vercel 与 Cloudflare

副标题：

> 一次完整的网站发布流程分享

讲稿：

> 今天我想分享的不是某个具体技术点，而是一个网站从本地开发到公网可访问的完整流程。这个流程包括前端页面怎么搭、怎么打包、怎么部署到 Vercel，以及为什么最后还要用 Cloudflare 做 DNS 托管来改善国内访问。

### 第 2 页：为什么做这个官网

页面内容：

1. 展示团队定位。
2. 展示研究方向、成员、成果和资源。
3. 提供对外交流和招新入口。
4. 建立一个可持续维护的信息窗口。

讲稿：

> 做官网的目的不是简单做一个页面，而是把团队的研究方向、问题库、成果和加入方式放到一个稳定入口里。它面向的不只是内部成员，也包括想了解我们的同学、老师、合作方和潜在加入者。

### 第 3 页：全流程总览

页面内容：

```
内容准备 -> 页面构建 -> 本地调试 -> Vite 打包
        -> Vercel 部署 -> 绑定域名 -> Cloudflare DNS -> 国内访问验证
```

讲稿：

> 整个过程可以看成八步。前四步发生在本地，目标是把网站做出来；后四步发生在线上，目标是让别人能稳定访问。中间的关键转折点是打包，打包之后项目就从开发工程变成了一组可以部署的静态文件。

### 第 4 页：项目技术选型

页面内容：

| 层次 | 选择 |
|---|---|
| 页面框架 | Vue 3 |
| 构建工具 | Vite |
| 路由 | Vue Router |
| 样式 | 原生 CSS |
| 部署 | Vercel |
| DNS | Cloudflare |

讲稿：

> 这里的选型都围绕一个目标：轻量、稳定、容易维护。这个网站不是复杂系统，所以没有引入后端和数据库，而是使用静态数据和静态部署。这样可以减少维护成本，也更适合实验室展示型官网。

### 第 5 页：页面结构

页面内容：

首页、团队、成果、动态、项目、资源、加入我们、详情页。

讲稿：

> 页面不是随便分的，而是按照访问者的需求路径组织的。一个人第一次进入网站，先看首页了解整体定位，再进入团队页看人，进入成果页看论文，进入项目页看问题，最后如果感兴趣，可以进入加入我们页面。

### 第 6 页：数据驱动设计

页面内容：

```
siteData.js
  -> 首页统计
  -> 成员
  -> 论文
  -> 项目
  -> 新闻
  -> 资源
```

讲稿：

> 这个项目最重要的设计是内容集中管理。页面只负责展示，数据文件负责内容。这样后续更新成员或论文时，不需要进入每个页面里找位置，只需要改统一的数据源。

### 第 7 页：组件化

页面内容：

| 组件 | 复用位置 |
|---|---|
| Header | 全站顶部 |
| Footer | 全站底部 |
| PageHero | 各页面标题区 |
| HeroVisual | 首页视觉 |

讲稿：

> 组件化的意义是减少重复。比如导航栏每页都有，如果复制到每个页面里，修改一次导航就要改很多地方。现在它是一个组件，改一次全站生效。

### 第 8 页：本地开发与调试

页面内容：

```bash
npm run dev
```

检查项：

1. 页面是否打开。
2. 跳转是否正确。
3. 手机端是否适配。
4. 内容是否完整。

讲稿：

> 本地开发阶段主要是快速试错。修改页面后马上在浏览器里看到效果，这个阶段关注的是页面本身是否可用，而不是马上考虑部署。

### 第 9 页：Vite 打包

页面内容：

```bash
npm run build
```

输出：

```
dist/
  index.html
  assets/
  images/
```

讲稿：

> 打包可以理解成把开发工程导出成浏览器能直接访问的文件。线上部署时，Vercel 实际发布的不是我们的 `.vue` 文件，而是打包后的 HTML、CSS、JS 和图片。

### 第 10 页：为什么选择 Vercel

页面内容：

1. 自动构建。
2. 自动 HTTPS。
3. 静态站支持好。
4. 免费额度够用。
5. Git 推送后可自动上线。

讲稿：

> Vercel 适合这类前端项目，因为它能把很多运维工作自动化。我们不需要自己租服务器、配 Nginx、申请证书，只需要告诉它怎么构建、发布哪个目录。

### 第 11 页：Vercel 部署流程

页面内容：

```
Git 仓库
  -> Vercel 导入
  -> npm install
  -> npm run build
  -> 发布 dist
  -> 获得线上地址
```

讲稿：

> 部署流程的关键是自动化。第一次配置完成后，后续更新网站就变成了提交代码，Vercel 自动构建并发布。

### 第 12 页：部署中的 404 问题

页面内容：

问题：

```
/people 刷新后 404
```

解决：

```json
{
  "rewrites": [
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
```

讲稿：

> 这是单页应用部署里很典型的问题。前端路由里的 `/people` 不是真实文件，所以服务器需要统一返回入口页面，再由前端决定显示哪个页面。这个配置就是告诉 Vercel：所有路径都交给前端应用处理。

### 第 13 页：为什么还需要 Cloudflare

页面内容：

Vercel 解决部署，Cloudflare 解决访问入口。

| 平台 | 作用 |
|---|---|
| Vercel | 放网站、构建、发布 |
| Cloudflare | DNS、代理、缓存、HTTPS、安全 |

讲稿：

> 网站部署成功只是第一步。真正让用户访问时，还需要一个稳定的域名入口。Cloudflare 在这里负责 DNS 托管和代理，让访问路径更可控，也能改善默认域名在国内访问不稳定的问题。

### 第 14 页：DNS 是什么

页面内容：

```
用户输入 imblab.cn
  -> DNS 查询
  -> 找到网站所在位置
  -> 返回页面
```

讲稿：

> DNS 可以理解为互联网的地址簿。用户记住的是域名，但浏览器真正连接的是服务器或 CDN 地址。DNS 的作用就是把域名翻译成可访问的目标。

### 第 15 页：Cloudflare 配置流程

页面内容：

1. 添加站点。
2. 修改域名 NS。
3. 添加 DNS 记录。
4. 在 Vercel 添加自定义域名。
5. 等待验证。
6. 多网络测试。

讲稿：

> 这一部分的流程看起来有点绕，但本质只有两件事：第一，把域名解析权交给 Cloudflare；第二，让 Cloudflare 和 Vercel 都知道这个域名应该指向当前网站。

### 第 16 页：访问链路

页面内容：

```
用户
  -> imblab.cn
  -> Cloudflare DNS / Proxy
  -> Vercel
  -> 网站内容
```

讲稿：

> 用户访问时，看到的是我们的自定义域名。请求先到 Cloudflare，再由 Cloudflare 转到 Vercel。如果静态资源已经被缓存，Cloudflare 还可以直接返回一部分内容。

### 第 17 页：国内访问的处理

页面内容：

1. 不直接使用 `vercel.app` 默认域名作为主入口。
2. 使用 `.cn` 自定义域名。
3. DNS 托管到 Cloudflare。
4. 开启代理和缓存。
5. 多网络环境测试。

讲稿：

> 国内访问不是只看服务器部署成功，还要看域名解析、网络链路和缓存策略。当前方案的优势是成本低、配置快，适合实验室官网。如果未来要求更高，可以考虑备案后接入国内 CDN。

### 第 18 页：踩坑总结

页面内容：

| 问题 | 解决 |
|---|---|
| 二级页面刷新 404 | Vercel rewrites |
| 图片大 | WebP、懒加载、压缩 |
| 默认域名国内不稳 | 自定义域名 + Cloudflare |
| 内容维护麻烦 | 数据集中管理 |

讲稿：

> 这些坑都说明一个问题：网站上线是一个完整链路，不是单点技术。页面、构建、部署、路由、域名、网络环境，每一层都可能影响最终访问体验。

### 第 19 页：成本与维护

页面内容：

| 项目 | 成本 |
|---|---|
| Vue / Vite | 免费 |
| Vercel | 免费额度 |
| Cloudflare | 免费计划 |
| 域名 | 年费 |
| 服务器 | 无需单独购买 |

讲稿：

> 这个方案最大的优势是低成本和低维护。除了域名年费，其他部分基本可以用免费额度支撑实验室展示站。这对科研团队和学生项目很友好。

### 第 20 页：后续优化

页面内容：

1. 图片转 WebP / AVIF。
2. 路由懒加载。
3. 内容从 Excel 自动生成。
4. 增加 SEO 元信息。
5. 接入访问统计。
6. 未来可考虑 CMS。

讲稿：

> 当前版本已经跑通了从页面构建到上线访问的主流程。后续优化主要集中在性能、内容维护和数据分析上，也就是让网站更快、更好维护、更能反馈访问效果。

### 第 21 页：一句话总结

页面内容：

> 用 Vue 和 Vite 把内容做成静态官网，用 Vercel 快速部署，用 Cloudflare 管理域名和访问链路，最终形成一个低成本、可维护、可访问的实验室官网。

讲稿：

> 如果用一句话总结，这个项目不是单纯写了一个网页，而是完整跑通了“内容组织、页面构建、静态打包、自动部署、域名解析、国内访问优化”的上线闭环。

---

## 十、现场演示建议

如果组会允许现场演示，可以安排 3-5 分钟。

### 演示 1：展示本地项目结构

打开项目目录，重点展示：

1. `src/views`：页面。
2. `src/components`：公共组件。
3. `src/data/siteData.js`：内容数据。
4. `vercel.json`：部署路由配置。

讲法：

> 这里不用看具体代码，只看分工。页面、组件、数据、部署配置是分开的，所以后续维护会更清楚。

### 演示 2：改一条数据，全站更新

可以演示修改一个统计数字、成员姓名或新闻标题。

讲法：

> 这体现了数据驱动的好处。不是去页面里找文字，而是在统一数据文件里维护内容。

### 演示 3：访问二级页面并刷新

例如访问：

```
/people
/join
/projects
```

然后刷新页面。

讲法：

> 如果这个配置没做好，刷新就可能 404。现在能正常刷新，说明 Vercel 的 rewrites 配置生效。

### 演示 4：展示 Cloudflare DNS 记录

如果不方便展示账号后台，可以放截图。

重点标出：

1. 域名。
2. CNAME 记录。
3. 代理状态。
4. SSL/TLS 状态。

讲法：

> 这一步把“网站能部署”变成“用户能通过正式域名访问”。

---

## 十一、可能被问到的问题

### Q1：为什么不用 WordPress 或现成建站工具？

答：

> WordPress 更适合内容频繁发布、有后台编辑需求的网站。这个项目目前更像一个静态展示官网，数据量可控，使用 Vue + Vite 可以获得更高的页面定制能力和更低的运行成本。后续如果非技术成员维护需求变强，可以再接入 CMS。

### Q2：为什么不直接部署到国内服务器？

答：

> 国内服务器访问会更稳定，但需要购买服务器、配置环境、处理备案、维护安全和证书。这个项目当前阶段更看重快速上线和低维护成本，所以先选择 Vercel + Cloudflare 的方案。如果未来访问规模和稳定性要求更高，可以迁移到国内云服务或 CDN。

### Q3：Cloudflare 一定能保证国内访问吗？

答：

> 不能把它说成绝对保证。它能改善默认域名访问不稳定、提供更好的 DNS 和代理能力，但不同地区、运营商和网络策略仍可能有差异。对实验室官网来说，这个方案性价比很高；如果要求强稳定，最终还是要考虑备案和国内 CDN。

### Q4：网站内容以后怎么更新？

答：

> 当前主要通过 `siteData.js` 集中维护，后续可以从 Excel 表格生成数据文件，或者接入无头 CMS。核心思路是让内容和页面结构分离，减少维护难度。

### Q5：为什么不用后端和数据库？

答：

> 因为目前没有登录、评论、实时查询等动态需求。用后端会增加复杂度和维护成本。展示型官网优先静态化，可以更快、更安全、更便宜。

### Q6：这个项目有哪些可以继续优化的地方？

答：

> 图片压缩、路由懒加载、SEO、访问统计、内容自动化和后台编辑都是后续方向。当前版本先完成主链路，后续再逐步增强。

---

## 十二、分享时的表达技巧

### 12.1 少说“用了什么技术”，多说“解决什么问题”

不建议这样讲：

> 我用了 Vue、Vite、Vue Router、Vercel、Cloudflare。

建议这样讲：

> 页面太多，需要组件化，所以用了 Vue；本地开发和打包要快，所以用了 Vite；网址要对应不同页面，所以用了 Router；网站要快速上线，所以用了 Vercel；国内访问和域名解析要更稳定，所以用了 Cloudflare。

### 12.2 把技术链路讲成用户访问链路

不要只从开发者视角讲：

```
npm run build -> dist -> deploy
```

可以从用户视角讲：

```
用户输入域名
  -> DNS 找到入口
  -> Cloudflare 接收请求
  -> Vercel 返回页面
  -> 浏览器渲染官网
```

这样非技术同学更容易理解。

### 12.3 每一部分都回答三个问题

讲每个环节时都可以用这三个问题组织：

1. 这一环节要解决什么问题？
2. 我们用了什么方式？
3. 这样做带来了什么好处？

例如讲 Vercel：

1. 问题：本地网站怎么给别人访问？
2. 方式：Vercel 自动构建和部署。
3. 好处：不用自己维护服务器，推送代码后自动上线。

---

## 十三、最终总结稿

可以在结尾这样说：

> 这次官网建设最重要的收获，不是学会某一个框架，而是跑通了一个完整的网站上线流程。前端部分解决内容如何组织和展示，Vite 解决如何把开发工程变成静态文件，Vercel 解决如何快速部署到公网，Cloudflare 解决域名解析、代理和访问稳定性。  
>
> 对实验室官网这种展示型项目来说，这套方案的优势是低成本、低维护、上线快，同时也保留了后续扩展空间。后续如果内容维护变复杂，可以接入 Excel 自动生成或 CMS；如果国内访问要求更高，可以考虑备案和国内 CDN。  
>
> 所以这个项目可以概括为一句话：用轻量前端技术完成页面构建，用自动化平台完成部署，用 DNS 和代理服务打通访问链路，最终把一个本地项目变成真正可对外使用的实验室官网。

---

## 十四、可直接放在 PPT 的流程图

```mermaid
flowchart LR
  A["内容准备<br/>成员 / 论文 / 项目 / 动态"] --> B["siteData.js<br/>统一数据源"]
  B --> C["Vue 页面<br/>views + components"]
  C --> D["Vite 构建<br/>npm run build"]
  D --> E["dist 静态文件"]
  E --> F["Vercel 部署<br/>自动构建 + HTTPS"]
  F --> G["自定义域名<br/>imblab.cn"]
  G --> H["Cloudflare DNS<br/>解析 + 代理 + 缓存"]
  H --> I["用户访问<br/>国内网络验证"]
```

---

## 十五、最短版复述

如果时间不够，可以压缩成 3 分钟版：

> 这个官网采用 Vue 3 + Vite 构建，核心思路是把页面、组件和数据分开。页面负责结构，组件负责复用，`siteData.js` 负责全站内容，所以后续更新成员、论文和项目会比较方便。  
>
> 本地开发完成后，通过 `npm run build` 把 Vue 工程打包成 `dist` 目录，也就是浏览器能直接访问的 HTML、CSS、JS 和图片。然后使用 Vercel 部署，Vercel 会自动安装依赖、执行构建、发布静态文件，并提供 HTTPS 和 CDN。  
>
> 因为这个项目使用前端路由，直接刷新 `/people` 这类页面可能 404，所以在 `vercel.json` 里配置 rewrites，让所有路径都返回 `index.html`，再由 Vue Router 接管页面显示。  
>
> 最后，为了解决默认域名在国内访问不稳定和正式入口的问题，我们绑定自定义 `.cn` 域名，并把 DNS 托管到 Cloudflare。Cloudflare 负责域名解析、代理、缓存和安全层，用户访问域名时先经过 Cloudflare，再回源到 Vercel。这样就形成了从内容准备、前端构建、自动部署到国内访问优化的完整闭环。

