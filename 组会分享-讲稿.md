# 从零搭建实验室官网 —— 一次完整的上线之旅

> 组会分享讲稿 | 建议时长 25-30 分钟

---

## 今天讲什么

给实验室做了一个官网。今天不聊技术细节，就想跟大家分享一下：一个网站是怎么从什么都没有，到最后任何人用手机都能打开的。

我会按时间线来讲 —— 先做了什么，后做了什么，中间遇到了什么问题，怎么解决的。

---

## 一、先看成品

**打开网站，边滚动边说。**

这是首页。上面是实验室的名字和四个研究方向的概览数字，往下滚动是四个方向的具体介绍，再往下是代表性的研究问题、精选论文和最新动态。

点几个页面给大家看看：团队页、成果页、加入我们页。

整个网站没有任何后端服务，没有数据库。就是一个纯静态的网站，所有页面都是提前生成好的。

网站有两个访问地址：一个 Vercel 免费送的域名，一个是咱们自己的 `imblab.cn`。为什么有两个？后面会讲，这跟国内访问有关。

**想让大家记住的第一件事：从头到尾没花一分钱。** 四个免费服务就撑起来了：前端框架是开源的，构建工具是开源的，部署平台有免费额度，DNS 和 CDN 也是免费的。

---

## 二、动手之前：选什么路子

做网站第一步是选技术。市面上的选择太多了，各种框架、各种工具。但我的思路是够用就行，不做多余的事情。

**为什么选了 Vue？** 不是因为它比 React 好，而是我比较熟。它的文件组织方式很直观 —— 一个页面就是一个文件，结构、样式、逻辑全放在一起，改起来方便。

**为什么没用现成的组件库？** Element Plus、Ant Design 这些，做出来一看就是后台管理系统。实验室官网应该有学术的气质。所以所有样式全部自己写，按钮、卡片、导航栏、配色，一点一点调出来的。

**为什么没有后端和数据库？** 实验室网站的内容更新不频繁。成员变动、新论文发表、新项目立项，一个月顶多改几次。为了这个搭一个后端服务加数据库，太重了。把内容直接写在文件里，改的时候改文件，重新生成，重新发布，反而更省事。

**整个项目多大？** 十来个页面文件，一个数据文件管所有内容，一个样式文件管所有外观，再加上一个六行的部署配置。一个人，大概一周多时间。

---

## 三、内容怎么管理：最核心的一个设计决定

### 所有内容集中在一个地方

网站上的每一个字 —— 实验室介绍、四个研究方向、四十多个成员、二十八个研究问题、论文列表、新闻、资源 —— 全部来自同一个数据文件。

这个设计的好处是什么？

**改一个地方，全站自动更新。** 比如芦老师的研究方向是"控制理论"，参与的是第 1 号和第 5 号问题。这些信息写在一起。团队页展示芦老师的时候，自动显示他参与了哪些问题。反过来，打开第 5 号问题的详情页，自动显示有哪些成员在研究它。两边的数据是一致的，因为来源是同一个。

如果以后芦老师接了新问题，只需要改一个地方。团队页、问题详情页、首页的统计数字，全部自动更新，不会遗漏。

### 真正的数据源头是 Excel

这些数据最开始不在代码文件里。实验室有一份 Excel 表格，成员信息、研究问题、论文都在里面。

写了一个自动转换脚本，读 Excel，生成代码文件。所以不写代码的人也能维护网站内容 —— 打开 Excel 填一行，跑一下转换，重新发布。不用碰代码。

### 用户打开网站的体验

因为数据是提前生成好的，用户打开网站不需要等后端返回数据。没有 loading 动画，没有"加载失败请重试"。打开那一瞬间，所有内容就已经在页面上了，速度很快。

---

## 四、页面是怎么规划的

一共十个页面。不逐一讲了，挑几个有代表性的说。

### 首页

从上到下的信息结构：实验室名称 → 四个数字（方向数、问题数、成员数、年度） → 四大方向介绍 → 代表性研究问题 → 精选论文 → 最新动态 → 加入入口。

这样布局的考虑是：一个第一次来的人，往下翻一遍，实验室做什么的、有哪些方向、出了什么成果，全部看完。不用点来点去。

首页顶部还有个小的动态效果，几根线和几个节点在动，看起来比较有生气。没有用什么复杂的动画库，就是浏览器自带的动画能力实现的。

### 团队页

成员按四个研究方向分组。顶部有筛选标签 —— 全部、负责人、控制理论、无人系统、人工智能、工程项目。点一下只看对应方向的人。

每个成员的卡片用方向对应的颜色来标注。蓝色的就是控制理论方向，青色的就是人工智能方向。颜色的区分让人不用读文字就能知道谁在哪个方向，扫一眼就有概念。

### 成果页

论文列表，支持按标题、作者、会议名称搜索。每篇论文可以一键复制 BibTeX 引用格式，点了之后显示"已复制"。

### 加入我们页

这个页面花的时间最多。不是因为实现复杂，而是要反复想"一个学生看到这个页面，他最想知道什么？"

最终的结构：
- 最上面是一段话，告诉学生对实验室能获得什么
- 然后是三种身份选择：本科科研训练、研究生长期课题、工程协作者
- 再往下是四个加入的理由
- 然后是申请流程，分四步讲清楚
- 材料清单，可以打勾，勾选后会显示准备完成度百分比。四个全勾完就变绿，提示"可以投递了"
- 邮件模板，点击直接打开邮件客户端，收件人和标题都预填好了
- 最后是常见问题，点一下展开答案

材料清单的进度条是个小细节。让学生感觉自己在完成一项任务，有个明确的完成度，比干巴巴的"请准备以下材料"好很多。

---

## 五、让网站上线

### 从开发到可以部署

本地开发的时候，改一行代码，浏览器自动刷新，很方便。但要放到服务器上让别人访问，需要做一次构建 —— 把所有东西打包成浏览器能直接打开的静态文件。

构建完的结果很简单：一个入口文件，一个 JS 文件，一个 CSS 文件，几张图片，一个 Logo。不算图片，所有代码加起来大概 300 多 KB，压缩后在手机上用流量打开不到一秒。

### 部署到哪里

这些文件要放到服务器上。传统的做法是买云服务器、装服务器软件、配安全证书、配防火墙、传文件……每年花几百上千块，还要操心别挂掉、别过期。

我选了一个叫 Vercel 的平台，专门做前端网站托管的。免费额度每月 100GB 流量，实验室网站够用了。把代码推到 GitHub，后面全是自动的：自动构建、自动部署、自动发到全球 CDN。从点下推送到用户能看到新版本，大概半分钟。

### 踩的第一个坑：刷新就出错

部署完开开心心打开网站，首页正常。点导航到团队页，正常。然后手贱刷新了一下 —— 页面变成 404 找不到。

什么原因？网站的 URL 像 `/people` 这样很干净，但服务器上并没有一个叫 `/people` 的文件。浏览器刷新时向服务器要这个文件，服务器说我没有，就报 404 了。

解决也简单，加一个部署配置，写六行，意思是"不管用户请求什么路径，都给入口文件，让前端自己去判断该显示哪个页面"。加上之后重新部署，好了。

这个问题不解决的话，用户收藏了团队页的链接，下次打开就是 404。所以这六行虽然少，但是必须的。

---

## 六、国内访问的问题

### 自己打得开，不代表别人也能

部署完拿到 Vercel 送的免费域名，我自己打开，一切正常。发给校外的朋友试，他说打不开。换个网络，也打不开。

查了之后才知道，Vercel 的默认域名在国内访问有问题。DNS 解析可能被干扰，他们的 CDN 节点在国内也很少。自己觉得做好了，不代表用户用得上。

### 解决思路：加一个中转层

换了思路。不用 Vercel 给的域名，用我们自己注册的 `imblab.cn`。

但光换域名不够，网站的文件还是放在 Vercel 的服务器上（在境外）。需要一个中转 —— Cloudflare。

Cloudflare 是什么？简单说，它是全球最大的网络服务商之一，在全球到处都有节点，包括中国大陆。在它上面做了一些配置之后，访问变成了这样：

> 用户访问 imblab.cn → 连到 Cloudflare 在国内附近的节点（很快） → Cloudflare 去境外 Vercel 取内容 → 返回给用户

用户感知不到东西在境外。他访问的是一个 `.cn` 域名，连的是国内附近的节点，速度很快。至于 Cloudflare 怎么去 Vercel 取内容，用户不需要知道。

打个比方：东西存在境外的仓库。用户在境内跟一个中转站打交道，中转站离用户很近，它负责跑去境外拿东西。

### 实际操作五个步骤

第一，在 Cloudflare 注册，添加域名。第二，去域名注册商那里把 DNS 服务器改成 Cloudflare 的。第三，在 Cloudflare 添加解析记录，指向 Vercel，打开代理开关 —— 这步最关键，打开了流量才会走中转。第四，在 Vercel 后台加上自定义域名。第五，等生效，测试。

全部配完大概半小时。这之后再测试，无论什么网络环境都能正常打开了。

而且顺便多了几个好处：SSL 安全证书自动管理，不用自己买和续期；静态资源缓存到边缘节点，第二次打开更快；文件自动压缩，传输更省流量。

---

## 七、完整回顾

### 一周时间线

```
前两天：搭骨架
  - 初始化项目
  - 配置页面路由
  - 建好目录结构

中间两天：写页面
  - 首页、团队页、成果页、动态页、问题库页
  - 写整体样式，定配色方案
  - 加动画效果

后两天：打磨细节
  - 资源页、加入我们页、各详情页
  - 交互细节：复制、筛选、进度条
  - 手机端适配

最后一天：部署上线
  - 构建打包
  - Vercel 部署
  - 发现刷新 404 → 加配置解决
  - 发现国内打不开 → 加 Cloudflare 解决
```

### 完整链路，用一句话串起来

> 内容在 Excel 里维护，转成数据文件 → 前端页面读取数据，生成网站 → 构建工具打包成静态文件 → 部署平台分发到全球 → DNS 服务做中转实现国内访问 → 用户在任何网络、任何设备上都能打开。

### 五个关键选择

| 选择 | 考虑 |
|------|------|
| 不用后端和数据库 | 内容量不大，更新不频繁，静态文件更简单可靠 |
| 不用现成的 UI 组件库 | 要有自己的视觉风格，不能看起来像管理后台 |
| 数据集中管理 | 改一处全站更新，不遗漏，好维护 |
| 用 Vercel 部署 | 免费、省心、自动安全证书 |
| 加 Cloudflare 做 DNS | 解决国内访问，顺便加速和加固 |

---

## 八、几点体会

**第一，做出一个能用的东西，比学一堆技术更重要。** 这个项目用的全是最基础的东西，没有什么高深的技术。但这些基础的东西组合在一起，就能做出一个真实可用的网站。先做出来，再谈优化。

**第二，从写完到能用，中间还有不少事。** 做页面大概花了三天，调试部署和解决国内访问又花了差不多同样的时间。写代码的时候觉得自己做完了，实际上"用户能正常打开"才叫做完。

**第三，站在用户的角度去测。** 如果我没有在手机上用流量打开，就不会发现图片太大加载慢。如果我没有请校外的朋友帮忙试，就不会发现境外域名打不开。自己觉得好了不算数。

### 以后可以做的

- 图片换个格式，减少六七成的体积，手机上打开更快
- 搞一个可视化后台，不写代码的成员也能直接在网页上改内容
- 加上访问统计，看看哪些页面访问得多

---

## 总结一句话

> 四个免费服务，一周多时间，一个从零到上线、国内能正常访问的实验室官网。

---

## 如果被问到

**"为什么不直接用 WordPress 那种现成的建站工具？"**

确实可以，而且更快。不过自己做有两个好处：代码在自己手里，想怎么改都行；而且实验室同学也能参考这个项目了解前端开发是怎么回事。

**"维护成本高不高？"**

很低。部署平台和安全证书都是自动的，不用管。内容更新就是改 Excel → 跑转换 → 重新发布，五分钟搞定。唯一的开销是域名续费，一年几十块。

**"安全吗？"**

因为是纯静态网站，没有数据库、没有用户登录、没有提交表单，天然就比较安全。最大的风险是账号被盗或者域名被劫持，这种靠强密码和两步验证就能防住。
