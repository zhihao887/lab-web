# 组会分享讲解脚本 —— 逐章节话术与演示指南

> 配套文件：`组会分享-实验室官网技术实现.md`（技术详细内容）
> 本文件：逐章节怎么讲、什么时候演示、重点怎么强调

---

## 开场（2 分钟）

### 话术

> "大家好，今天我来分享一下咱们实验室官网的技术实现。从写第一行代码到用户能在浏览器里打开 imblab.cn，整个链路我都走了一遍。今天不讲高深的理论，就讲一件事 —— 怎么用四个免费服务，从零搭起一个完整的、国内能访问的实验室官网。"

### 建立预期

> "我会按这个顺序讲：先看前端代码怎么组织的，再说怎么构建打包，然后讲怎么部署到 Vercel，最后讲怎么用 Cloudflare 搞定国内访问。大概 25 分钟，中间随时可以打断提问。"

### 要点
- 用"四个免费服务撑起一个官网"作为贯穿全场的叙事线索
- 提前说清楚时长，大家心里有底

---

## 第一章：项目概览（3 分钟）

### 页面定位怎么讲

打开网站首页，边滚动边说：

> "先看看最终效果。这就是咱们实验室的官网，纯静态站点，没有后端，没有数据库。首页从上到下是 —— Hero 大图、研究概览统计、四个研究方向、代表性研究问题、精选成果、最新动态。"

滚动时在每个区域停顿 2 秒，让大家看清楚。

### 技术栈怎么讲

切换到技术栈表格，用一个对比来突显"极简"：

> "说到前端，大家可能听过 React、Next.js、Tailwind 这些名词。我们这个项目的选择非常克制 —— 看依赖列表，核心就两个：Vue 3 和 Vue Router。"

停顿一下，指着表格说：

> "没有 UI 组件库，所有样式手写。没有 TypeScript，纯 JavaScript。没有状态管理库，所有数据一个文件搞定。为什么这么选？学术实验室网站需要独特的设计气质，组件库千篇一律。而且内容量可控，不需要 CMS 那一套。"

### 过渡到第二章

> "那具体代码怎么组织的？我们来看前端架构。"

---

## 第二章：前端页面构建（10 分钟）

这是最长的章节，需要拆成几个小段来讲。

### 2.1 项目初始化（30 秒，快速带过）

> "初始化就两条命令，Vite 脚手架 + Vue Router。Vite 是 Vue 的作者尤雨溪做的构建工具，开发服务器秒启动，比 Webpack 快一个数量级。"

**重点强调**：不用讲命令本身，讲为什么选 Vite（快）。

### 2.2 目录架构（2 分钟）

打开 VS Code 展示文件树（这是最直观的）：

> "看 src 目录，这是全部源码。结构非常扁平 —— 数据放 data，公共组件放 components，页面放 views，样式放 styles。一共 14 个组件文件。"

**演示动作**：
1. 打开 `src/data/siteData.js`，滚动让大家看到文件的长度（3070 行）
2. 说："这是整个项目最重要的文件，等一下细讲"
3. 打开 `src/views/`，逐个文件名读一遍
4. 打开 `src/styles/main.css`，滚动一下

> "大家注意看，10 个页面、4 个公共组件、1 个数据文件、1 个样式文件。没有嵌套目录，没有复杂模块。"

### 2.3 数据驱动架构（3 分钟，核心重点）

这是全场最重要的设计决策，需要讲透。

**第一步：展示数据结构**

打开 `src/data/siteData.js`，定位到各个 export：

> "这个文件导出了什么？siteInfo 网站基本信息、researchDirections 四个研究方向、people 成员列表 40 多人、problems 28 个研究问题、publications 论文、news 动态、resources 资源。网站所有内容都在这里。"

**第二步：展示数据关联**

指向一个成员对象：

> "看，芦安洋老师这个对象里，有一个 `problems` 字段，数组里是 problem-001、problem-005。这什么意思？表示芦老师关联了这两个研究问题。这种关联关系在 JS 里就是一个字符串数组，非常简单。"

**第三步：解释为什么这样设计**

> "这样做有四个好处：
> 1. 非技术人员也能维护 —— 大家的数据其实维护在 Excel 表格里，我用 Python 脚本自动转成 JS
> 2. 改一处全站生效 —— 比如改芦老师的关联问题，团队页和问题详情页同步更新
> 3. 结构清晰不出错 —— 每个字段的含义一目了然
> 4. 构建时内联，运行时零 API 调用 —— 用户打开网站，数据已经在 JS 文件里了，不需要等后端返回"

**第四步：演示数据流动**

打开 HomeView.vue，找到 `import { ... } from '@/data/siteData.js'`：

> "每个页面组件直接从 siteData 导入需要的数据。比如首页导入 directions, problems, publications, news。Vue 的响应式系统自动处理渲染，数据变了页面就变。"

### 2.4 路由设计（1.5 分钟）

打开 `src/router/index.js`：

> "10 条路由配置，非常简单。但我重点要讲的是这个 —— `createWebHistory()`。"

指着这行代码：

> "Vue Router 有两种模式：Hash 模式和 History 模式。Hash 模式的 URL 带 # 号，比如 `/index.html#/people`，丑，而且 SEO 不友好。History 模式的 URL 干净 —— `/people`，跟普通网站一样。"

停顿，制造悬念：

> "但 History 模式有一个坑。用户直接在地址栏输入 `/people` 然后回车，浏览器会向服务器请求 `/people` 这个文件。但我们的服务器上没有这个文件，只有 `index.html`。所以会返回 404。"

> "这个问题怎么解决？等一下讲到部署的时候揭晓。现在先记住 —— History 模式需要服务器配合。"

### 2.5 样式体系（1.5 分钟）

**重点演示毛玻璃效果**：

打开网站，指着固定在顶部的导航栏：

> "大家看导航栏，能透过它隐约看到下面的内容。这就是现在很流行的毛玻璃效果，CSS 一行搞定 —— `backdrop-filter: blur(18px)`。"

打开 `src/styles/main.css`，定位到 `:root` 的 CSS 变量：

> "注意到没有，我们没有用具体颜色值，全都是 CSS 变量。`--ink` 主文字、`--muted` 次要文字、`--blue`、`--teal`、`--amber`、`--rose`。这四个颜色分别对应四个研究方向，每个方向的卡片、标签、边框都用不同的主题色。"

打开 PeopleView 页面，展示四个方向的卡片：

> "控制理论用蓝色，人工智能用青色，无人系统用琥珀色，工程项目用玫红。用户一看到颜色就知道是哪个方向的内容，不需要读文字。"

### 2.6 关键功能实现（1.5 分钟）

挑两个最有趣的功能演示：

**演示 BibTeX 复制**：

打开成果详情页，点一下"复制 BibTeX"按钮，显示复制成功提示：

> "成果详情页有个 BibTeX 一键复制功能。实现也很简单，用浏览器原生的 `navigator.clipboard.writeText()`。注意我还写了降级方案，如果浏览器不支持 Clipboard API —— 比如老版本浏览器或者 HTTP 环境 —— 会自动降级到创建 textarea + execCommand 的方案。这就是前端开发的防御性编程。"

**演示加入我们的材料清单**：

打开 JoinView 页面，勾选材料清单的 checkbox，进度条变化：

> "加入我们页面有个材料清单，勾选后会显示完整度百分比。实现用了 Vue 3 的 `computed` 计算属性 —— 计算勾选数量除以总数。不需要手动更新 DOM，数据一变，进度条自动变。这就是响应式框架的好处。"

### 过渡到第三章

> "好，前端代码讲完了。接下来我们把它变成用户能访问的网站。第一步，构建打包。"

---

## 第三章：Vite 构建与优化（3 分钟）

### 话术

> "构建用的 Vite，本质上是对 Rollup 的封装。配置简单到什么程度？就这几行。"

打开 `vite.config.js`，指着只有 6 行的配置：

> "就一个插件，Vue 支持。没了。跟 Webpack 几十上百行的配置比起来，Vite 的开箱即用体验好太多了。"

### 构建产物分析

在终端执行 `ls -lh dist/assets/`，展示文件大小：

> "构建结果在 dist 目录。JS 262KB，CSS 60KB。加起来 322KB，gzip 压缩后大约 80KB。在 4G 网络下传输不到 1 秒。图片稍微大一点，三张 PNG 合计 5.6MB，这个是后面需要优化的点。"

### 演示构建速度

在终端执行 `npm run build`，展示构建时间：

> "看构建时间 —— 大概 8 秒。对比一下 Webpack 动辄 30 秒起步的构建速度，Vite 的优势很明显。"

### 优化方向（快速提）

> "有四个可以优化的方向，这里快速说一下：图片转 WebP 大概能减 70% 体积；路由懒加载让首页只加载需要的组件；Critical CSS 内联首屏样式；预加载关键资源。这四个做完，首屏性能还能提升一截。"

---

## 第四章：Vercel 部署（5 分钟）

### 4.1 为什么选 Vercel？（1 分钟）

> "部署平台有很多选择 —— 可以买阿里云服务器自己配 Nginx，可以用 GitHub Pages，也可以用 Netlify、Vercel 这类平台即服务。"

> "我们选 Vercel 有三个原因：
> 1. **免费** —— 100GB 带宽每月，实验室网站流量完全够
> 2. **零运维** —— 自动 HTTPS 证书、自动 CDN、自动续期
> 3. **对 SPA 友好** —— 天然支持 History 模式的 rewrite 规则"

> "如果自己买服务器配 Nginx，要处理防火墙、端口、SSL 证书续期、CDN 配置、负载均衡……Vercel 把这些全干了。"

### 4.2 Rewrite 规则 —— 部署配置的核心（2 分钟，重点讲）

这是全场唯一需要深入讲的技术点，因为它解决了前面埋的坑。

> "还记得前面说的 History 模式 404 问题吗？现在揭晓答案。"

打开 `vercel.json`：

> "就这 6 行 JSON。`source: /:path*` 匹配所有路径，`destination: /index.html` 指定目标。什么意思？任何路径请求，Vercel 都返回 index.html。然后 Vue Router 在浏览器端接管，根据 URL 渲染对应的组件。"

画图解释（可以在白板上或口述）：

> "用户访问 `/people` → Vercel 服务器收到请求 → 发现没有 `/people` 文件 → 匹配到 rewrite 规则 → 返回 `/index.html` → 浏览器执行 JS → Vue Router 读取 URL `/people` → 匹配到 PeopleView 组件 → 渲染页面。"

> "如果没有这一条规则，直接访问 `/people` 就是 404。这就是 SPA 部署最核心的一个配置。"

### 4.3 部署流程演示（2 分钟）

如果可以现场演示，最直观：

> "部署有两种方式。最简单的是用 Vercel CLI，两条命令。我给大家演示一下……当然实际已经部署好了，我用 --dry-run 模拟一下。"

或者展示流程图，逐一解释：

> "整个部署流程从 git push 到全球 CDN 上线，大约 30 秒。Vercel 自动检测到 main 分支有推送，自动执行 npm build，自动部署到全球边缘网络。"

### 4.4 引出下一个问题

> "部署成功后会得到一个 vercel.app 的域名。看着一切完美了。但问题来了 —— 这个域名在国内能正常访问吗？"

停顿，摇头：

> "不行。vercel.app 在国内 DNS 解析不稳定，经常打不开。这就引出我们的最后一步 —— Cloudflare DNS。"

---

## 第五章：Cloudflare DNS 实现国内访问（6 分钟）

### 5.1 问题分析（1 分钟）

> "先讲为什么 vercel.app 在国内不行。三个原因："

在黑板上或口述画三条：

> "第一，DNS 污染 —— 国内的 DNS 服务器可能返回错误的 IP 地址。第二，GFW 干扰 —— vercel.app 属于境外域名，网络层面可能被阻断。第三，CDN 节点 —— Vercel 在国内的边缘节点很少，即使连上了速度也慢。"

### 5.2 解决方案原理（2 分钟，重点）

画架构图：

> "解决方案是加一层 Cloudflare DNS 代理。"

```
没有 Cloudflare：
  用户 → DNS 查 vercel.app → 可能解析失败 → 打不开

加了 Cloudflare：
  用户 → DNS 查 imblab.cn → Cloudflare DNS 正常解析
       → Cloudflare 边缘节点 → Vercel 源站 → 返回内容
```

> "关键变化是什么？把访问入口从 vercel.app 换成了我们自己的域名 imblab.cn。.cn 域名在国内 DNS 完全正常，不会被拦截。Cloudflare 在全球（包括中国）有边缘节点，提供 CDN 加速。流量经过 Cloudflare 代理，相当于在境外和境内之间架了一座桥。"

用一个比喻：

> "可以这么理解：Vercel 的服务器在境外，vercel.app 的 DNS 也在境外。大陆用户想直接过去，路上有各种障碍。Cloudflare 相当于在边境设立了一个中转站，用户先到中转站（国内可以正常访问的节点），中转站再去境外取东西，拿回来给用户。用户根本不知道东西在境外。"

### 5.3 操作步骤（2 分钟，快速过）

> "具体操作分五步："

快速过每一张配置截图（或者对着文档念）：

> "第一步，在 Cloudflare 添加站点，输入域名。第二步，去域名注册商那里把 DNS 服务器改成 Cloudflare 的。第三步，添加两条 CNAME 记录指向 Vercel。第四步，在 Vercel 添加自定义域名。第五步，验证。其中最关键的是第三步 —— DNS 记录的代理状态必须开橙色云朵，流量才会走 Cloudflare 网络。"

### 5.4 推荐配置（1 分钟，快速过）

> "有几个推荐配置值得设置：
> - SSL/TLS 加密模式选 Full，保证两端都是 HTTPS
> - 给 /assets/* 和 /images/* 设置缓存规则，静态资源缓存一个月
> - 开启 Auto Minify 和 Brotli 压缩
> - Rocket Loader 建议关掉，它对 Vue 这种 JS 框架有兼容问题"

### 过渡到第六章

> "好，到这里四个步骤都讲完了。我们回过头来看一下全链路。"

---

## 第六章：全链路回顾（2 分钟）

### 话术

展示完整的全链路图，从左到右用手势引导：

> "我们从左到右回顾一遍："

> "**开发阶段** —— 数据定义在 siteData.js，14 个 Vue 组件引用数据，Vite 构建打包成 dist/ 目录的静态文件。"

> "**部署阶段** —— Git push 推送到 GitHub，Vercel 自动触发构建，产物分发到全球 CDN。"

> "**DNS 层** —— imblab.cn 域名通过 Cloudflare DNS 解析，CNAME 指向 Vercel，同时开启 Cloudflare 代理。"

> "**用户访问层** —— 用户浏览器发请求，DNS 返回 Cloudflare 边缘节点 IP，边缘节点检查本地缓存，有就直接返回，没有就去 Vercel 源站取，缓存起来再返回。"

> "**这四层加起来，一个完整的上线链路就闭环了。**"

### 技术决策回顾

展示决策表，快速逐行读出：

> "最后回顾一下六个关键技术决策：
> - 选 Vue 3 因为上手快，SFC 单文件组件直观
> - 选 Vite 因为开发体验好，秒级热更新
> - 纯 CSS 不用框架，因为需要完全的设计自由度
> - 静态数据不用 CMS，因为内容量可控
> - 选 Vercel 部署，因为免费且零运维
> - 选 Cloudflare DNS，因为解决国内访问问题"

---

## 第七章：踩坑记录（3 分钟）

### 话术风格

这一章用轻松的语气讲，可以穿插个人体验：

> "最后分享几个踩过的坑，希望大家以后少走弯路。"

### 坑一：History 模式 404

> "第一个坑就是刚才反复强调的 History 模式 404。这个坑的表现是 —— 开发环境一切正常，部署上线后，从首页点进去没问题，但刷新页面或者直接输入 URL 就是 404。当时纠结了好久，以为是 Vercel 的问题，最后发现是需要 vercel.json 配 rewrite。加六行代码就解决了。"

### 坑二：大图拖慢移动端

> "第二个坑是图片。三张 PNG 合计 5.6MB，在电脑上没什么感觉，但在手机上用 4G 打开，等了 5 秒白屏。后来加了 loading=lazy 延迟加载，但没有转 WebP。转 WebP 可以减 60-70% 体积，这个还没做，是一个 TODO。"

### 坑三：国内访问

> "第三个坑就是国内访问。一开始用 vercel.app 域名，我自己能打开，以为没问题。后来有同学说打不开，才发现 vercel.app 在国内有访问问题。加了 Cloudflare + imblab.cn 域名后才解决。教训：测试的时候不能只看自己能不能打开，要考虑到不同的网络环境。"

### 坑四：数据维护

> "第四个坑不是技术问题是流程问题。成员信息、论文列表经常要改，每次都改 JS 代码很麻烦。现在用 Excel 做数据源，Python 脚本转成 JS。但如果要真正让非技术成员也能自己更新，最好还是接入一个无头 CMS，比如 Strapi 或者 TinaCMS，给一个可视化的后台界面。"

---

## 第八章：总结（2 分钟）

### 话术

> "总结一下今天的分享："

> "我们用 Vue 3 + Vite 构建了一个纯静态 SPA，14 个组件、10 个页面、3070 行数据文件。部署用 Vercel，一个 6 行 JSON 配置解决 SPA rewrite。DNS 用 Cloudflare，加一层代理解决国内访问问题。"

> "**四个免费服务 —— Vue 生态（开源免费）、Vite（开源免费）、Vercel（免费额度足够）、Cloudflare（免费计划足够）—— 撑起一个完整的、国内可访问的实验室官网。零服务器成本，零运维负担。**"

### 收尾

> "如果大家想了解某个部分的更多细节，或者想也给自己的项目搭一套类似的，会后可以来找我讨论。谢谢大家。"

---

## 附：时间分配速查表

| 章节 | 分配时间 | 累计时间 | 关键词 |
|------|----------|----------|--------|
| 开场 | 2 min | 2 min | 叙事线索建立 |
| 一、项目概览 | 3 min | 5 min | 网站展示 + 技术栈对比 |
| 二、前端构建 | 10 min | 15 min | **数据驱动架构是重点** |
| ├ 2.1 初始化 | 0.5 min | | 快速带过 |
| ├ 2.2 目录 | 2 min | | VS Code 展示文件树 |
| ├ 2.3 数据架构 | 3 min | | **最重点，讲透** |
| ├ 2.4 路由 | 1.5 min | | 抛出 History 404 悬念 |
| ├ 2.5 样式 | 1.5 min | | 毛玻璃 + 四色主题 |
| └ 2.6 功能 | 1.5 min | | BibTeX + 材料清单演示 |
| 三、构建优化 | 3 min | 18 min | 构建速度 + 产物分析 |
| 四、Vercel 部署 | 5 min | 23 min | **Rewrite 规则是重点** |
| 五、Cloudflare DNS | 6 min | 29 min | **代理原理是重点** |
| 六、全链路回顾 | 2 min | 31 min | 四层架构串联 |
| 七、踩坑记录 | 3 min | 34 min | 轻松语气，个人经验 |
| 八、总结 | 2 min | 36 min | 四个免费服务的叙事收束 |
| Q&A | 自由 | | |

---

## 附：演示检查清单

分享前确认以下准备：

- [ ] 浏览器打开网站首页，清理缓存后加载正常
- [ ] VS Code 打开项目，提前展开关键目录
- [ ] 终端准备就绪，`npm run build` 可随时执行
- [ ] `vercel.json` 文件内容已打开
- [ ] Cloudflare Dashboard 已登录（可选，截图备用更安全）
- [ ] 全链路图（第六章）截图或准备好画板
- [ ] 手机也能打开网站（验证国内访问确实正常）
- [ ] 如果现场网络不稳定，准备离线截图备用
