# 实验室官网技术实现 —— 从零到上线全链路分享

> 适用场景：组会技术分享 | 时长建议：25-35 分钟  
> 关键词：Vue 3 · Vite · Vercel · Cloudflare DNS · 国内访问 · 纯静态站点

---

## 一、项目概览

**项目名称**：问题研究社实验室官网（Problem Research Lab）  
**代码仓库**：本地 Git 管理，单分支 `main`，16 次提交  
**线上地址**：通过 Vercel 部署 + Cloudflare DNS 托管自定义域名

### 1.1 网站定位

一个**纯静态**的学术实验室展示型站点，核心诉求：

| 诉求 | 实现方式 |
|------|----------|
| 展示研究方向 | 四大方向 + 28 个研究问题 |
| 展示成员 | 40+ 成员按方向分组 |
| 展示学术成果 | 论文列表 + BibTeX 一键复制 |
| 招募新人 | 材料清单 + 邮件模板 + FAQ |
| 资源沉淀 | 数据集/代码/演示/文档入口 |

### 1.2 技术选型一览

```
┌──────────────────────────────────────────────────┐
│                   技术栈全景                       │
├───────────────┬──────────────────────────────────┤
│ 前端框架      │ Vue 3 (Composition API)           │
│ 构建工具      │ Vite 6                            │
│ 路由         │ Vue Router 4 (History 模式)        │
│ 样式         │ 纯 CSS + CSS 自定义属性             │
│ 组件库       │ 无 (全部手写)                       │
│ 状态管理      │ 无 (静态数据驱动)                   │
│ TypeScript   │ 无 (纯 JavaScript)                 │
│ 部署平台      │ Vercel                            │
│ DNS 托管     │ Cloudflare                        │
│ 域名         │ imblab.cn                         │
└───────────────┴──────────────────────────────────┘
```

---

## 二、前端页面构建

### 2.1 项目初始化

```bash
npm create vite@latest labweb -- --template vue
cd labweb
npm install vue-router@4
```

极简依赖，`package.json` 只有两个核心依赖：

```json
{
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.5"
  }
}
```

**为什么不用 UI 组件库？** 学术实验室网站需要独特的学术气质，组件库千篇一律。手写 CSS 获得完全的设计自由度。

---

### 2.2 项目目录架构

```
src/
├── main.js                  # 入口：createApp + use(router)
├── App.vue                  # 根组件：Header + <router-view> + Footer
├── router/index.js          # 10 条路由配置
├── data/siteData.js         # ★ 核心：3070 行全部站点数据
├── components/              # 4 个公共组件
│   ├── LabHeader.vue        #   固定导航栏（毛玻璃）
│   ├── SiteFooter.vue       #   页脚
│   ├── PageHero.vue         #   页面标题 Hero
│   └── HeroVisual.vue       #   首页 SVG 动效
├── views/                   # 10 个页面组件
│   ├── HomeView.vue         #   首页
│   ├── PeopleView.vue       #   团队页
│   ├── PublicationsView.vue #   成果页（搜索+筛选）
│   ├── NewsView.vue         #   动态页
│   ├── ProjectsView.vue     #   问题库页
│   ├── ResourcesView.vue    #   资源页
│   ├── JoinView.vue         #   加入我们（800行 scoped CSS）
│   ├── ResearchDetailView.vue    #   研究方向详情
│   ├── PublicationDetailView.vue #   成果详情（BibTeX 复制）
│   └── ProjectDetailView.vue     #   问题详情
└── styles/main.css          # 3179 行全局样式
```

---

### 2.3 数据驱动架构

这是整个项目最核心的设计决策：**所有页面内容集中在一个数据文件中**。

`src/data/siteData.js` 结构：

```javascript
export const siteInfo = {
  name: '问题研究社',
  nameEn: 'Problem Research Lab',
  email: 'contact@imblab.cn',
  description: '...',
  // ...
}

export const researchDirections = [
  {
    id: 'control-theory',
    name: '控制理论',
    theme: 'blue',
    description: '...',
  },
  // 4 个方向
]

export const people = [
  {
    id: 'lu-anyang',
    name: '芦安洋',
    role: 'director',
    direction: 'control-theory',
    problems: ['problem-001', 'problem-005'],
    // ...
  },
  // 40+ 成员
]

export const problems = [
  {
    id: 'problem-001',
    title: '...',
    direction: 'control-theory',
    status: 'active',
    year: 2025,
    description: '...',
    vision: '...',
    value: '...',
    challenge: '...',
    explorationSpace: '...',
    progress: '...',
    // ...
  },
  // 28 个研究问题
]

export const publications = [ /* 论文列表 */ ]
export const news = [ /* 动态列表 */ ]
export const resources = [ /* 资源列表 */ ]
```

**这种架构的好处**：
1. **非技术人员也能维护** —— 数据文件来自 Excel 表格，Python 脚本自动转换
2. **单文件修改** —— 改一处，全站生效
3. **类型安全可追加** —— 即便不用 TypeScript，结构清晰不会出错
4. **构建时零请求** —— 数据在构建时内联，无运行时 API 调用

---

### 2.4 路由设计

```javascript
// router/index.js
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  { path: '/',                    name: 'home',        component: HomeView },
  { path: '/people',              name: 'people',      component: PeopleView },
  { path: '/research/:slug',      name: 'research-detail', component: ResearchDetailView },
  { path: '/publications',        name: 'publications', component: PublicationsView },
  { path: '/publications/:id',    name: 'publication-detail', component: PublicationDetailView },
  { path: '/news',                name: 'news',        component: NewsView },
  { path: '/projects',            name: 'projects',    component: ProjectsView },
  { path: '/projects/:id',        name: 'project-detail', component: ProjectDetailView },
  { path: '/resources',           name: 'resources',   component: ResourcesView },
  { path: '/join',                name: 'join',        component: JoinView },
  { path: '/:pathMatch(.*)*',     redirect: '/' },     // 404 兜底
]

const router = createRouter({
  history: createWebHistory(),    // ★ History 模式，URL 干净无 #
  routes,
  scrollBehavior() {
    return { top: 0 }             // 每次导航回到顶部
  },
})

// 动态设置页面标题
router.afterEach((to) => {
  const pageTitle = getPageTitle(to.name)  // 从路由名映射中文标题
  document.title = `${pageTitle} - ${siteInfo.name}`
})
```

**关键点**：使用 `createWebHistory()` 而非 `createWebHashHistory()`，URL 干净美观（`/people` 而非 `/#/people`），但这要求服务器必须配置 SPA fallback —— 所有路径都返回 `index.html`。

---

### 2.5 样式体系

#### 2.5.1 CSS 自定义属性（Design Tokens）

```css
:root {
  /* 颜色体系 */
  --ink: #172033;          /* 主文字 */
  --muted: #5f6f84;        /* 次要文字 */
  --soft: #eef5fb;         /* 柔和背景 */
  --line: #dce8f2;         /* 边框 */
  --panel: rgba(255,255,255,0.82);  /* 面板 */
  --blue: #4f82c2;         /* 主题蓝 */
  --blue-deep: #2f5f98;
  --teal: #1fb7b1;
  --amber: #f2b84b;
  --rose: #e56b6f;
  --green: #55a86a;

  /* 设计常数 */
  --shadow: 0 18px 55px rgba(23,32,51,0.12);
  --radius: 8px;

  /* 字体栈 */
  font-family: Inter, "SF Pro Display", "PingFang SC",
               "Microsoft YaHei", system-ui, sans-serif;
}
```

#### 2.5.2 设计亮点

| 特性 | 实现 | 位置 |
|------|------|------|
| 毛玻璃导航栏 | `backdrop-filter: blur(18px)` | `LabHeader.vue` |
| 渐变主按钮 | `linear-gradient(135deg, var(--blue-deep), var(--teal))` | `main.css` |
| 网格背景 | `body::before` 伪元素 46px 网格 + 渐变 | `main.css` |
| 卡片悬浮动效 | `translateY(-2px)` + 阴影增强 + 边框高亮 | `main.css` |
| 研究方向配色 | 四大方向各有 blue/teal/amber/rose 主题 | `siteData.js` |
| 响应式三断点 | 1100px / 820px / 560px | `main.css` |
| 减少动画偏好 | `@media (prefers-reduced-motion)` | `main.css` |

#### 2.5.3 动画体系

```css
/* SVG 线条绘制动画 —— 首页 Hero */
@keyframes drawLine { to { stroke-dashoffset: 0; } }

/* 节点脉冲 —— 首页 SVG */
@keyframes pulseDot { 0%,100% { opacity:1; } 50% { opacity:.35; } }

/* 渐变流动 —— 成员卡片 */
@keyframes gradientShift { 0% { background-position: 0% 50%; } ... }

/* 环形旋转 —— 负责人头像 */
@keyframes ringRotate { to { transform: rotate(360deg); } }

/* 卡片淡入 —— 列表页 */
@keyframes cardFadeIn { from { opacity:0; transform:translateY(12px); } ... }
```

---

### 2.6 关键功能实现

#### 成果页 —— BibTeX 一键复制

```javascript
// PublicationDetailView.vue
async function copyBibtex() {
  try {
    await navigator.clipboard.writeText(pub.value.bibtex)
    copied.value = true
    setTimeout(() => copied.value = false, 2000)
  } catch {
    // 降级方案
    const textarea = document.createElement('textarea')
    textarea.value = pub.value.bibtex
    document.body.appendChild(textarea)
    textarea.select()
    document.execCommand('copy')
    document.body.removeChild(textarea)
  }
}
```

#### 加入我们页 —— 材料清单完整度追踪

```javascript
const checklist = reactive({
  resume: false,
  transcript: false,
  personalStatement: false,
  recommendation: false,
})

const completeness = computed(() => {
  const items = Object.values(checklist)
  const done = items.filter(Boolean).length
  return Math.round((done / items.length) * 100)
})
```

#### 团队页 —— 分类筛选

```javascript
const activeFilter = ref('all')

const filteredPeople = computed(() => {
  if (activeFilter.value === 'all') return people
  if (activeFilter.value === 'directors') return people.filter(p => p.role === 'director')
  return people.filter(p => p.direction === activeFilter.value)
})
```

---

## 三、Vite 构建与优化

### 3.1 构建配置

`vite.config.js` —— 极简到极致：

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
})
```

### 3.2 构建命令

```bash
npm run build    # vite build → dist/
npm run preview  # vite preview --host 127.0.0.1 → 本地预览生产构建
```

### 3.3 构建产物

```
dist/
├── index.html                           # 入口 HTML
├── logo.svg                             # Favicon
├── assets/
│   ├── index-7-vivahq.css   (60 KB)     # 全局 CSS
│   └── index-DOrRnt2N.js    (262 KB)    # 应用 JS
└── images/
    ├── lab-hero-computational-biology.png  (1.7 MB)
    ├── lab-ai-discovery-workspace.png      (1.9 MB)
    └── lab-team-discussion.png            (1.8 MB)
```

| 指标 | 数值 | 说明 |
|------|------|------|
| JS Bundle | 262 KB | Vue 3 + Router + 全部数据 + 组件 |
| CSS Bundle | 60 KB | 全局样式 + 动画 + 响应式 |
| JS+CSS (gzip) | ~80 KB | 实际网络传输量 |
| 图片 | 5.6 MB | 未压缩 PNG，优化空间大 |
| 首屏时间 | < 1s | 无 API 请求，全静态 |

### 3.4 可优化的点

1. **图片压缩**：PNG → WebP/AVIF，预计缩小 70-80%
2. **路由懒加载**：`() => import('./views/HomeView.vue')`，减少首屏 JS
3. **Critical CSS**：内联首屏关键样式
4. **预加载关键资源**：`<link rel="preload">`

---

## 四、Vercel 部署

### 4.1 为什么选 Vercel？

| 考量 | Vercel 表现 |
|------|------------|
| 静态站点支持 | 原生支持，零配置 |
| HTTPS | 自动 Let's Encrypt 证书 |
| CDN | 全球边缘网络 |
| 部署速度 | `git push` 后 30 秒上线 |
| 免费额度 | 100GB 带宽/月，个人/实验室足够 |
| SPA 支持 | Rewrites 配置完美解决 History 模式 |

### 4.2 部署配置

`vercel.json`：

```json
{
  "rewrites": [
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
```

这 6 行是整个部署的核心。什么意思？

```
用户访问 /people
  → Vercel 服务器没有 /people 这个文件
  → Rewrites 规则生效
  → 返回 /index.html
  → Vue Router 接管路由
  → 渲染 PeopleView 组件
```

**没有这个配置会怎样？** 直接访问 `/people` 返回 404，只有从首页点击跳转才能正常显示（因为那是客户端路由）。

### 4.3 部署流程

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ 本地开发  │ ──→ │ Git Push │ ──→ │ Vercel   │ ──→ │ 全球 CDN │
│ npm run  │     │ to main  │     │ 自动构建  │     │ 边缘节点  │
│   dev    │     │          │     │ npm build│     │ 分发上线  │
└──────────┘     └──────────┘     └──────────┘     └──────────┘

时间线：
  git push                  0s
  Vercel 检测到推送          2s
  npm install               15s
  npm run build             8s
  部署到边缘网络             5s
  ─────────────────────────────
  总计                       ~30s
```

### 4.4 通过 Vercel CLI 部署（推荐）

```bash
# 安装 Vercel CLI
npm i -g vercel

# 首次部署（交互式配置）
vercel
# ? Set up and deploy? Y
# ? Which scope? <你的团队>
# ? Link to existing project? N
# ? What's your project's name? labweb
# ? In which directory is your code? ./
# ? Want to override settings? N

# 后续部署
vercel --prod    # 一键上线
```

也可以连接 GitHub 仓库实现自动部署：每次 push 到 `main` 分支自动触发构建和发布。

### 4.5 Vercel 默认域名

部署成功后 Vercel 会自动分配一个域名：`labweb.vercel.app`

**问题来了**：`vercel.app` 域名在中国大陆访问不稳定（DNS 污染 + CDN 节点受限）。

这就是为什么需要下一步 —— Cloudflare DNS 托管。

---

## 五、Cloudflare DNS 托管实现国内访问

### 5.1 问题分析

```
中国用户访问 vercel.app 域名的困境：

用户 ──→ DNS 解析 vercel.app
           │
           ├── 可能被污染，解析到错误 IP
           ├── 可能被拦截，返回空响应
           └── 即使解析成功，Vercel CDN 在国内节点有限
```

### 5.2 解决方案架构

```
          ┌──────────────────────────────────────┐
          │          Cloudflare DNS              │
          │  imblab.cn  ──→  cname.vercel-dns.com │
          └──────────────────────────────────────┘
                          │
          ┌───────────────┴───────────────┐
          │                               │
    国内用户                          国外用户
  DNS 解析正常                      DNS 解析正常
  Cloudflare CDN 加速               Vercel 全球 CDN
  （部分缓存）                      （直接响应）
```

### 5.3 具体操作步骤

#### 第一步：在 Cloudflare 添加站点

1. 注册/登录 [Cloudflare](https://dash.cloudflare.com/)
2. 点击 "Add a Site" → 输入 `imblab.cn`
3. 选择 Free 计划（个人/实验室足够）
4. Cloudflare 会扫描现有 DNS 记录

#### 第二步：修改域名 DNS 服务器

在域名注册商（如阿里云、腾讯云）处，将 DNS 服务器改为 Cloudflare 分配的：

```
NS: elsa.ns.cloudflare.com
NS: lee.ns.cloudflare.com
```

这一步生效需要 **24-48 小时**（实际通常 1-2 小时）。

#### 第三步：添加 DNS 记录

在 Cloudflare DNS 面板添加：

| 类型 | 名称 | 目标 | 代理状态 |
|------|------|------|----------|
| CNAME | `@` | `cname.vercel-dns.com` | 🌐 已代理 |
| CNAME | `www` | `cname.vercel-dns.com` | 🌐 已代理 |

**关键**：开启橙色云朵（Proxied），流量走 Cloudflare 网络。

#### 第四步：在 Vercel 添加自定义域名

```bash
# 在项目根目录执行
vercel domains add imblab.cn
vercel domains add www.imblab.cn
```

或在 Vercel Dashboard → Settings → Domains → 添加 `imblab.cn` 和 `www.imblab.cn`。

#### 第五步：验证与等待

```bash
# 验证 DNS 解析
dig imblab.cn
# 应该返回 Cloudflare 的 IP（因为开启了代理）

# 验证 SSL 证书
curl -I https://imblab.cn
# 应该返回 200 OK，证书由 Cloudflare 签发
```

### 5.4 为什么这套方案能解决国内访问？

```
请求链路对比：

【仅 Vercel】
用户 ──→ DNS (vercel.app 不稳定) ──→ Vercel CDN (国内节点少)
                                           │
                                     可能超时/失败

【Vercel + Cloudflare DNS】
用户 ──→ Cloudflare DNS (.cn 域名正常) ──→ Cloudflare Edge (中国有节点)
                                                    │
                                              Vercel Origin (回源)
                                                    │
                                              正常返回内容
```

**核心优势**：
1. `.cn` 域名在国内 DNS 解析完全正常，不受干扰
2. Cloudflare 在中国大陆有边缘节点，提供 CDN 加速
3. Cloudflare 的 SSL 证书被国内运营商信任
4. 即使 Vercel 被限制，流量经过 Cloudflare 代理可以绕过

### 5.5 Cloudflare 推荐配置

**SSL/TLS 设置**：
```
SSL/TLS → Overview → 加密模式选 "Full" 或 "Full (strict)"
```

**缓存规则**：
```
Caching → Cache Rules → 创建规则：
  路径: /assets/*   (静态资源)
  缓存 TTL: 1 month

  路径: /images/*
  缓存 TTL: 1 month
```

**速度优化**：
```
Speed → Optimization →
  ✓ Auto Minify (JS, CSS, HTML)
  ✓ Brotli 压缩
  ✓ Early Hints
  ✓ Rocket Loader (可选，对 Vue 可能有兼容问题，建议关闭)
```

---

## 六、全链路回顾

### 6.1 从代码到用户浏览器

```
┌─────────────────────────────────────────────────────────────┐
│                        开发阶段                              │
│                                                             │
│  siteData.js  ──→  Vue 组件  ──→  Vite 构建  ──→  dist/   │
│  (3070行数据)      (14个组件)      (打包压缩)       (静态文件) │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                        部署阶段                              │
│                                                             │
│  Git Push  ──→  Vercel CI  ──→  CDN 分发  ──→  vercel.app  │
│                (自动构建)       (全球边缘)                    │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                        DNS 层                                │
│                                                             │
│  imblab.cn  ──→  Cloudflare DNS  ──→  Vercel Origin         │
│  (自定义域名)     (CNAME + Proxy)     (源站)                  │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                       用户访问                               │
│                                                             │
│  用户 → DNS解析 → Cloudflare Edge → Vercel → 返回页面        │
│  ↓                ↓ (命中缓存则跳过)                          │
│  < 1s             边缘缓存直接返回                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 技术决策汇总

| 决策 | 选择 | 替代方案 | 理由 |
|------|------|----------|------|
| 框架 | Vue 3 | React/Next.js | 上手快，单文件组件直观 |
| 构建 | Vite | Webpack | 开发体验好，构建快 |
| 样式 | 纯 CSS | Tailwind/Framework | 完全设计自由度 |
| 数据 | 静态 JS 文件 | CMS/后端 API | 内容量可控，无需数据库 |
| 部署 | Vercel | 阿里云/腾讯云 | 免费、零运维、自动 HTTPS |
| DNS | Cloudflare | 域名注册商自带 | 国内访问优化、免费 CDN |

---

## 七、踩坑记录与经验

### 7.1 Vue Router History 模式的 404

**现象**：直接访问 `/people` 刷新页面返回 404。

**原因**：服务器没有 `/people` 这个物理文件，SPA 需要所有路径返回 `index.html`。

**解决**：`vercel.json` 配置 rewrites 规则（见第四章）。

### 7.2 图片过大拖累首屏

**现象**：三张 PNG 合计 5.6MB，移动端首屏加载超过 5 秒。

**改进方向**：
- 转 WebP 格式（质量不变，体积减 60-70%）
- 使用 `<img loading="lazy">` 延迟加载非首屏图片
- 使用响应式图片 `srcset` 适配不同屏幕

### 7.3 国内访问 Vercel 不稳定

**现象**：`labweb.vercel.app` 在中国大陆间歇性无法访问。

**根因**：Vercel 默认域名在国内 DNS 解析存在问题。

**解决**：绑定自定义域名 + Cloudflare DNS 代理（见第五章）。

### 7.4 数据维护的挑战

**现象**：成员变动、论文更新需要频繁改代码。

**当前方案**：原始数据维护在 Excel 表格中，通过 Python 脚本生成 `siteData.js`。

**改进方向**：可以接入无头 CMS（如 Strapi/TinaCMS），让非开发人员通过后台界面更新内容。

---

## 八、总结

### 技术亮点

1. **极简架构**：2 个依赖（Vue + Vue Router），零运行时 API，纯静态站点
2. **数据驱动**：3070 行集中式数据文件，Excel ↔ JS 双向转换
3. **设计精良**：毛玻璃效果、SVG 动效、四色主题系统、响应式三断点
4. **SPA 路由**：History 模式 + Vercel Rewrites 完美解决刷新 404
5. **国内可访问**：Cloudflare DNS 代理 + `.cn` 域名，突破访问限制
6. **零运维成本**：Vercel + Cloudflare 免费套餐，自动续期 SSL

### 技术栈一句话总结

> **Vue 3 + Vite 构建纯静态 SPA，Vercel 部署，Cloudflare DNS 实现国内访问 —— 四个免费服务撑起一个完整的实验室官网。**

---

## 附录

### A. 本地开发命令

```bash
npm install          # 安装依赖
npm run dev          # 启动开发服务器 (localhost:5173)
npm run build        # 生产构建 (dist/)
npm run preview      # 预览生产构建 (localhost:4173)
```

### B. 部署命令速查

```bash
# Vercel CLI
vercel             # 预览部署
vercel --prod      # 生产部署
vercel domains add imblab.cn   # 添加自定义域名

# DNS 验证
dig imblab.cn
curl -I https://imblab.cn

# 清除 Cloudflare 缓存
# Dashboard → Caching → Configuration → Purge Everything
```

### C. 文件大小与性能

| 文件 | 原始 | Gzip | Brotli |
|------|------|------|--------|
| JS Bundle | 262 KB | ~75 KB | ~60 KB |
| CSS Bundle | 60 KB | ~12 KB | ~9 KB |
| HTML | 2 KB | ~1 KB | ~0.8 KB |
| **总计** | **324 KB** | **~88 KB** | **~70 KB** |

> 首屏网络传输量约 70-90 KB（Brotli 压缩），在 4G 网络下 < 1 秒完成加载。
